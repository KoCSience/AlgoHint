# Colab利用ガイド

Colabではリポジトリを配置して依存関係を準備した後、`uv run algohint --share` で起動できます。共有リンクは任意のPythonコードを実行する入口になります。自分だけがアクセスできる短時間の学習用途に限定し、URLを公開しないでください。

Colabのランタイムを終了すると `data/runtime/` のプロフィールログが失われる場合があります。継続利用したい記録は、秘密情報を含まないことを確認してから安全な個人ストレージへ退避してください。
