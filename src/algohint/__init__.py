"""AlgoHint Coach package.

The package keeps learning logic independent from the Gradio user interface so a
future desktop interface can reuse the same application services.
"""

from algohint.cli import main

__all__ = ["main"]
